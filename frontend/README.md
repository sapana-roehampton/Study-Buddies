## Frontend

In Sprint 3, the frontend is implemented using **Pug templates** within the backend.

The frontend components are located in:

- `backend/views` → Pug templates (UI pages)
- `backend/public` → CSS styling

The separate frontend folder is reserved for future development (e.g., React or static frontend).
